# Face Detection using OpenCV

This project detects faces in images using Python and OpenCV.

## Features
- Detects faces in multiple images
- Uses Haar Cascade Classifier
- Simple and beginner-friendly

## How to Run
1. Install OpenCV:
   pip install opencv-python

2. Run the program:
   python face_detector.py
